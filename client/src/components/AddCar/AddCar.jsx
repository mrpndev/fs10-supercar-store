import React from 'react'

export default function AddCar() {

	/* 
		? Add Car Component
		* utilize this component to add a car to our database
		* you will need to pass a token here
		* you will need to make an API call
		* build a form which will accept all of the data necessary
		* this component should be rendered when you click a button called "Add Car"
		* once adding a car is successful, make sure you render back AllCars
		* it means that this component must go away
		! Hard Mode
		* once complete, make sure it says "the car has been added", then disappear
		* what if you get an error from the server?
		* how would you handle input validation to respect schema constraints?
	*/
	return (
		<div>AddCar</div>
	)
}
